import { describe, expect, test } from "vitest"
import solve from "./index"

describe("day 04", () => {
    test("partA: example", async () => {
        const input = ""
        const result = await solve(input)
        expect(result.partA).toEqual(0)
    })
})
