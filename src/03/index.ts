const solve = async (
    input: string,
): Promise<{ partA: number; partB: number }> => {
    // TODO: Implement solution for day 3
    console.debug(input)
    return {
        partA: 0,
        partB: 0,
    }
}

export default solve
