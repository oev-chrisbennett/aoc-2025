const solve = async (input: string) => {
    const lines = input.split("\n").map((line) => line.trim())
    console.log(lines)
    return {
        partA: "-",
        partB: "-",
    }
}

export default solve
